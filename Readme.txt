Project Overview:

Use the arrow keys to play tetris! Left and right arrow keys move the blocks laterally. Up arrow key rotates the blocks. Spacebar auto drops the block. 

There is an executable JAR in the target folder. To run, you need Java 8 (to ensure compatibility with JavaFX)